export const environment = {
  production: true,
  /**
   * Backend API base URL on Render.
   * Replace with your Render backend URL (e.g. https://jj-store-api.onrender.com).
   * Use '' only if the frontend is served by the same backend (single Render service).
   */
  apiBaseUrl: 'https://YOUR-RENDER-BACKEND-URL',
};